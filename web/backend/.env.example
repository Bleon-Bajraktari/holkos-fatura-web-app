# ===========================================
# Holkos Fatura - Backend Environment
# ===========================================
# Kopjo këtë skedar në .env dhe plotëso vlerat

# ---------- DATABASE (TiDB Cloud) ----------
# Opsioni 1: DATABASE_URL i plotë (rekomandohet)
# Merr nga TiDB Cloud Console > Connect > Connection string
# Format: mysql://USER:PASSWORD@HOST:4000/DATABASE?sslaccept=strict
DATABASE_URL=mysql://USER:PASSWORD@HOST:4000/DATABASE?sslaccept=strict

# Opsioni 2: Variabla të ndara (automatsisht gjenerohen nga TiDB Vercel integration)
TIDB_HOST=
TIDB_PORT=4000
TIDB_USER=
TIDB_PASSWORD=
TIDB_DATABASE=test

# ---------- EMAIL (SMTP nga Cilësimet) ----------
# Plotëso Konfigurimin e Email-it në Cilësimet (smtp_server, smtp_user, smtp_password).
# Për Gmail: përdor App Password (jo fjalëkalimin normal).
# Localhost: backend përdor SMTP direkt.
# Deploy: Vercel function /api/send-email-smtp përdor SMTP (Render bllokon SMTP).

# ---------- AUTH (obligatorisht për deploy) ----------
# Për Render: vendos JWT_SECRET_KEY të fortë (min. 32 karaktere, random)
# Shembull: openssl rand -hex 32
JWT_SECRET_KEY=ndrysho-me-nje-sekret-të-fortë-min-32-karaktere

# Kredencialet fillestare (vetëm kur databaza është e re, pastaj ndrysho nga Cilësimet)
APP_INITIAL_USERNAME=admin
APP_INITIAL_PASSWORD=holkos2025

# ---------- CORS ----------
# CORS_ORIGINS=http://localhost:5173,https://localhost:5173

# Për zhvillim lokal me MySQL:
# MYSQL_HOST=localhost
# MYSQL_PORT=3306
# MYSQL_USER=root
# MYSQL_PASSWORD=
# MYSQL_DATABASE=holkos_fatura1
